version: "3.7"

services:
  im-com:
    container_name: im-com
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - CHOKIDAR_USEPOLLING=true
